# 课程人工整理安装与配置

1. 将本目录作为 MoviePilot 本地插件仓库挂载，并加入 `PLUGIN_LOCAL_REPO_PATHS`。
2. 重启 MoviePilot，安装并打开“课程自动整理”。
3. 进入 MoviePilot **设置 → 存储 & 目录**，确认电影、电视剧和儿童课程各有一条明确规则。
4. 三条规则应指向同一待处理来源；例如 `/media/incoming` 分别整理到 `/media/movies`、`/media/tv`、`/media/courses`。
5. 搬运方式、智能重命名、刮削、通知、覆盖策略和媒体库子目录均在 MoviePilot 规则中设置，不要在插件中重复配置。
6. 儿童课程规则名称或目标路径需明确包含“儿童”“课程”或 `courses`。
7. 若使用人工复核，关闭这些来源规则的自动监控，避免文件在确认前被自动整理。

打开插件后即进入待处理操作台：选择 TMDB 候选（如需要）、修改建议名称、选择媒体库并点击“确认整理”。没有可靠媒体关联时，系统会先要求按名称搜索关联；失败不会移除待处理条目。

安装和保存设置不会自动创建 MoviePilot 目录规则，不会修改 NAS 目录，也不会触发真实媒体移动。真实确认整理、部署、提交和推送均需单独授权。
